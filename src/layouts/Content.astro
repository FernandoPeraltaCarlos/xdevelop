<main class="main">
    <slot />
</main>
<section class="closeSession">
    <button id="close">Cerrar sesion</button>
</section>
<script>
    const token = localStorage.getItem('token');
    if(!token){
        window.location.href = '/';
    }

    function closeSession(){
        localStorage.removeItem('token');
        location.reload();
    }

    let button = document.querySelector('#close') as HTMLButtonElement;
    button.onclick = function (){
        closeSession();
    }

</script>
<style lang="scss" is:global>
    .main{
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        row-gap: 4em;
        padding: 0 0 2em 0;
    }
    .closeSession{
        width: 100%;
        padding: 2em;
        display: flex;
        justify-content: center;
        align-items: center;
        button{
            padding: 1em 2em;
            border:none;
            background-color: #535dfc;
            color:white;
            border-radius: 10px;
            &:hover{
                cursor: pointer;
            }
        }
    }
</style>